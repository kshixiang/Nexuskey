---
description: Nexuskey (Tauri) monorepo — frontend/backend boundaries and AppId discipline
alwaysApply: true
---

# Nexuskey 开发上下文

- **栈**：Vite + React + TypeScript 前端（`src/`），Rust **Tauri 2** 后端（`src-tauri/`）。包管理：`pnpm`。
- **本地运行**：`pnpm dev`（会先释放端口再起 `tauri dev`）。
- **应用标识**：前端 `AppId`（`src/lib/api/types.ts`）与后端 `AppType`（`src-tauri/src/app_config.rs`）必须一致；字符串为小写（如 `claude`、`codex`、`gemini`、`opencode`、`openclaw`、`hermes`）。
- **供应商模式**：Claude/Codex/Gemini 为切换写入 live 配置；OpenCode/OpenClaw/Hermes 为累加写入。新增客户端需在两端补齐枚举、`APP_IDS`、代理/同步/MCP 等相关分支。
- **改动原则**：优先沿用现有命名与文件结构；避免无关重构；业务文案走 i18n（`react-i18next`）。
